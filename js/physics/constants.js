// Re-export of physics constants from config — keeps physics decoupled from UI/canvas.

export {
  K_COULOMB,
  R_MIN_M,
  R_CAPTURE_M,
  UC_TO_C,
  NC_TO_C,
  UG_TO_KG,
  PX_TO_M,
  PIXELS_PER_CM,
} from '../config.js';
